{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/online-library/browser"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}

